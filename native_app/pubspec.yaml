name: native_app
description: >
  A command line application that can be compiled to native code using
  dart2native.

environment:
  sdk: '>=2.5.0 <3.0.0'

dev_dependencies:
  pedantic: ^1.8.0

executables:
  main: hello_world
