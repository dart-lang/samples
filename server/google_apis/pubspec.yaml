name: cloud_run_google_apis
publish_to: none # not intended for publication to pub.dev

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  googleapis_auth: ^1.1.0
  googleapis: ^2.0.0
  shelf: ^1.0.0
  shelf_router: ^1.0.0
