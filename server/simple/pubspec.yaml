name: container_server_example
publish_to: none # not intended for publication to pub.dev

environment:
  sdk: ^3.3.0

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.2
  shelf_static: ^1.1.0

dev_dependencies:
  http: ^1.2.0
  lints: ^3.0.0
  test: ^1.25.0
  test_process: ^2.1.0
