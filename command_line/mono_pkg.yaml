dart:
  - dev
  - stable

os:
  - linux
  - osx
  - windows

stages:
  - analyze:
    - dartanalyzer: --fatal-infos --fatal-warnings .
      os: linux
    - dartfmt:
      os: linux
  - unit_test:
    - test
